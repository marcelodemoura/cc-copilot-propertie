spring:
  datasource:
    url: jdbc:h2:mem:copilot;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never
  profiles:
    active: mock

api:
  key: test-key

indexer:
  base-path: .
